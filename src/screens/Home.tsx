import React from 'react'
import { View, Text } from 'react-native';

import Screen from '../components/Screen'

interface HomeProps {

}

export const Home = ({}) => {

    return (
      <Screen style={{}}>
        <Text style={{}}> Journal App in Progress </Text>
      </Screen>
    );
}